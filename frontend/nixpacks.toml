# Nixpacks configuration for frontend
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "sh -c 'npx serve -s dist -l ${PORT:-3000}'"
