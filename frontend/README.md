# Wallacast frontend

This is the React (Vite) frontend of Wallacast. It is just the web app UI. For everything else (setup, architecture, and deployment) see the root [../README.md](../README.md).

## Dev commands

```bash
npm install
npm run dev
```
